- content_for(:title, 'Home')
%div.jumbotron.container
  %h1 Lunch ordering
  %h2 Make your order for today)

  - if user_signed_in?
    %span Hello,
    = current_user.email
    = link_to 'Sign out', destroy_user_session_path, method:  :delete , class: "btn btn-large btn-primary"
  -else
    = link_to 'Sign Up', new_user_registration_path , class: "btn btn-large btn-primary"